==================================================================
ALPHA HUB — DOCUMENTAÇÃO DO SISTEMA / CHANGELOG
==================================================================
Projeto:   Alpha Hub (marketplace gratuito de solicitações/propostas)
Base:      Escopo Mestre (Alpha_Hub_Escopo_Mestre_150_Paginas.docx)
Local:     C:\Users\alpha\Desktop\Alpha-Hub\   (pasta PRÓPRIA na Área de Trabalho)
Obs.:      Projeto NOVO e SEPARADO do sistema Recy/Alpha Control.
           Recy = produção de inventário, em C:\Users\alpha\Desktop\INV - V1 (NÃO foi tocado).
           Em 2026-06-30 o projeto foi MOVIDO para esta pasta própria, a pedido do usuário.

------------------------------------------------------------------
2026-06-30 — Sessão 1: Fatia vertical do MVP (modo hard)
------------------------------------------------------------------

DECISÕES (alinhadas ao escopo):
- Stack: TypeScript full-stack — NestJS + Prisma + PostgreSQL (API)
  e Next.js 14 (App Router) + React 18 (web). [decisão do usuário]
- Entregável desta sessão: FATIA VERTICAL funcional ponta-a-ponta
  (cadastro/login -> publicar solicitação -> enviar proposta ->
  negociar no chat em tempo real -> aceitar proposta). [decisão do usuário]
- Ambiente: docker-compose (paridade) + PostgreSQL PORTÁTIL local,
  pois esta máquina não tinha Docker nem Node instalados. [decisão do usuário]
- Arquitetura: monólito modular, API-first, com trilha de auditoria
  (decisões fundadoras do escopo, Partes XVI/XVII). Sem paywall: listar
  solicitações é público; publicar/propor/conversar exige só conta.

AÇÕES EXECUTADAS:
1.  Lido e analisado o Escopo Mestre (20 partes / 160 capítulos).
2.  Instalado Node.js LTS v24.18.0 via winget (escopo de usuário, sem admin).
3.  Criado monorepo alpha-hub/ (apps/api, apps/web, docker-compose, scripts).
4.  API NestJS:
    - Domínios: identidade (auth JWT), marketplace (solicitações/propostas),
      comunicação (chat REST + WebSocket socket.io), comum (Prisma, auditoria,
      paginação, filtro de erros padronizado, Swagger em /api/docs).
    - Prisma schema: User, Organization, Membership, Consentimento(LGPD),
      Solicitacao, Proposta, Conversa, ParticipanteConversa, Mensagem, AuditLog.
    - Regras: autor não propõe à própria solicitação; 1 proposta por prestador;
      enviar proposta abre sala de negociação; só participante lê/escreve no chat;
      só o autor da solicitação aceita proposta.
5.  Web Next.js: home (feed + busca/filtro), login/cadastro, nova solicitação,
    detalhe (propostas + aceitar + chat em tempo real), minhas conversas.
    Design system próprio (globals.css), responsivo.
6.  Provisionado PostgreSQL 16.4 PORTÁTIL (binários EDB, sem Docker/sem admin):
    - Binários em %LOCALAPPDATA%\alphahub-pg ; dados em alpha-hub\.pgdata
    - Banco "alphahub" em localhost:5432 (usuário/senha alphahub/alphahub)
    - Script reutilizável: scripts/pg-portable.ps1 (start|stop|status)
7.  Prisma migrate (migração init aplicada) + seed (2 usuários demo + dados).
8.  Build da API (tsc/nest) e typecheck do web: OK.

VERIFICAÇÃO (evidências):
- Testes e2e: 12/12 PASSARAM (apps/api/test/fluxo.e2e-spec.ts) — cobrem o
  fluxo completo + caminhos de erro (validação, permissões, acesso ao chat).
- API no ar em http://localhost:3001/api — /api/health OK; login real
  retornou JWT para cliente@alphahub.dev.
- Web no ar em http://localhost:3000 — home renderiza dados reais (3 resultados);
  login no navegador via API (CORS) retornou 201; UI autenticada exibiu estado
  logado (Conversas / Nova solicitação / avatar / Sair) após /auth/me.

CORREÇÕES FEITAS DURANTE A SESSÃO:
- e2e: createApplication -> createNestApplication.
- tsconfig: excluído test/ e prisma/seed.ts do build para gerar dist/main.js
  na raiz (compatível com o Dockerfile).

CONTAS DE DEMONSTRAÇÃO (após seed):
- cliente@alphahub.dev   / alphahub123  (Marina Cliente)
- prestador@alphahub.dev / alphahub123  (Bruno Designer)

COMO RETOMAR / SUBIR DE NOVO (sem Docker):
  1) Postgres:  powershell scripts/pg-portable.ps1 start   (já fica de pé)
  2) API:       (em apps/api) node dist/main.js            (porta 3001)
  3) Web:       (em apps/web) next dev -p 3000             (porta 3000)
  Node está em:
  %LOCALAPPDATA%\Microsoft\WinGet\Packages\OpenJS.NodeJS.LTS_*\node-v24.18.0-win-x64

PRÓXIMOS PASSOS (roadmap do escopo):
- Indicações consentidas (Parte VII), reputação multidimensional (Parte X),
  comunidade/insights (Parte IX), busca/matching dedicados (Parte XII),
  moderação/admin (Parte XIV), monetização opcional (Parte XX).
==================================================================

------------------------------------------------------------------
2026-06-30 — Sessão 2: Mudança de pasta + roadmap completo (Partes VII–XIV)
------------------------------------------------------------------

DECISÕES / CORREÇÕES:
- O usuário esclareceu que o Alpha Hub NÃO deveria estar dentro de INV - V1
  (projeto de inventário). MOVIDO para pasta própria: C:\Users\alpha\Desktop\Alpha-Hub.
  Revertido o launch.json do INV - V1 (removida a entrada "web"); footprint limpo.
- Postgres portátil reiniciado apontando para Alpha-Hub\.pgdata (dados preservados).
- Autorizado "atacar tudo": implementadas as Partes VII, IX, X, XII e XIV.

NOVOS MÓDULOS (backend NestJS, todos com auditoria):
- Parte VII  Indicações: modelo Indicacao (origem, consentimento/LGPD, base legal,
  comissão, reserva 7 dias, anti-duplicidade por hash). Contato mascarado até reserva.
  Endpoints: POST/GET /indicacoes, GET :id, POST :id/reservar, PATCH :id/status.
- Parte X   Reputação multidimensional: modelo Avaliacao (comunicação/qualidade/prazo,
  1..5 + comentário) ligada a proposta ACEITA; agregado por dimensão (sem nota única).
  Endpoints: POST /avaliacoes, GET /usuarios/:id/reputacao, GET /avaliacoes/pendentes.
- Parte IX  Comunidade/Insights: Insight + voto (toggle) + comentários.
  Endpoints: GET/POST /insights, GET :id, POST :id/voto, POST :id/comentarios.
- Parte XII Busca & Matching: GET /matching/solicitacoes (recomendação por histórico,
  exclui próprias/já-propostas, ranking transparente) e GET /busca?q= (global).
- Parte XIV Moderação/Admin: papelSistema (USUARIO/MODERADOR/ADMIN) no JWT + PapeisGuard.
  Denuncia + Sancao. Endpoints: POST /denuncias; (staff) GET /admin/denuncias,
  PATCH /admin/denuncias/:id, POST /admin/sancoes, GET /admin/metricas.

FRONTEND (Next.js) adicionado:
- /comunidade (+ /comunidade/[id]), /indicacoes, /admin (métricas + denúncias).
- Componentes: Avaliar (estrelas), ReputacaoBadge, ReportButton (denúncia inline).
- Home: faixa "Recomendadas para você" (matching). Nav com Comunidade/Indicações/Admin.

BANCO: migração 20260630145110_fases_vii_a_xiv aplicada. Seed ampliado
(admin@alphahub.dev ADMIN; proposta aceita + avaliação; 1 indicação; 1 insight com voto+comentário).

VERIFICAÇÃO:
- Testes e2e: 24/24 PASSARAM (fluxo.e2e-spec 12 + fases.e2e-spec 12 cobrindo VII–XIV,
  incluindo caminhos de erro: LGPD sem consentimento=400, avaliação duplicada=409,
  não-admin no painel=403, mascaramento de contato, voto toggle, etc.).
- Build API e typecheck web: OK. API/Web/Postgres no ar (3001/3000/5432).
- Smoke test: /admin/metricas (usuarios 11, eventosAuditoria 29), /indicacoes, /insights.
- Páginas /, /comunidade, /indicacoes, /admin, /login respondem HTTP 200.

CONTA ADMIN (seed): admin@alphahub.dev / alphahub123  (papel ADMIN).
==================================================================

------------------------------------------------------------------
2026-06-30 — Sessão 3: Parte XI — Negociação, contratos e pagamentos
------------------------------------------------------------------

DECISÃO IMPORTANTE (segurança + escopo):
- Pagamento é ESCROW SIMULADO/registrado. A plataforma NÃO movimenta dinheiro real
  (camada futura/opcional do escopo). É uma máquina de estados auditável, sem
  integração financeira real.

BACKEND (módulo "negocios", com auditoria):
- Modelos: Contrato (escopo, valorTotal, prazoDias, status, assinaturas das 2 partes),
  Marco (entregável: titulo/valor/ordem/status), Pagamento (escrow simulado).
- Ciclo de vida: formalizar (de proposta ACEITA) -> RASCUNHO -> assinar (as 2 partes)
  -> ATIVO (cria escrow RETIDO por marco) -> prestador ENTREGA marco -> cliente APROVA
  (escrow RETIDO->LIBERADO, marco PAGO) -> quando todos pagos -> CONCLUIDO.
- Endpoints: GET /contratos, GET/POST /contratos[/:id], POST :id/assinar,
  POST :id/marcos/:marcoId/entregar, POST :id/marcos/:marcoId/aprovar.
- Regras: 1 contrato por proposta (unique), só partes acessam, papéis corretos
  para entregar/aprovar, contrato só ativa com as 2 assinaturas.

FRONTEND:
- Página /contratos (lista + gestão). Componentes ContratoView (assinar/entregar/
  aprovar/escrow) e FormalizarContrato (escopo + marcos dinâmicos).
- Integrado ao detalhe da solicitação: proposta ACEITA mostra "Formalizar contrato"
  ou o contrato existente. Link "Contratos" no menu.

BANCO: migração 20260630161033_parte_xi_contratos. Seed cria 1 contrato ATIVO
(220000, 2 marcos, escrow RETIDO) para a proposta aceita demonstrativa.

CORREÇÃO: em "assinar", o objeto retornado era montado antes de criar os pagamentos
(escrow vinha vazio); passou a re-buscar o contrato após criar o escrow. Também
ajustado um teste e2e (título de marco precisa ter >= 2 caracteres).

VERIFICAÇÃO:
- e2e: 30/30 PASSARAM (12 fluxo + 12 fases + 6 contratos), cobrindo o ciclo completo
  e erros (duplicar contrato=409, papel errado=403, ativar só com 2 assinaturas,
  terceiro sem acesso=403).
- Build API + typecheck web OK. Smoke: GET /contratos do cliente -> contrato ATIVO,
  valorTotal 220000, 2 marcos, 2 pagamentos RETIDO. Páginas /contratos e / -> HTTP 200.
==================================================================

------------------------------------------------------------------
2026-06-30 — Sessão 4: Parte XIII — Notificações, comunicação e retenção
------------------------------------------------------------------

BACKEND (módulo "notificacoes", @Global):
- Modelos: Notificacao (destinatário, categoria, tipo, título, corpo, link, lida)
  e NotificacaoPreferencia (toggle por categoria: proposta/mensagem/contrato/
  indicacao/comunidade/moderacao).
- NotificacoesService.notificar() respeita a preferência da categoria e é tolerante
  a falhas (nunca derruba o fluxo de negócio). NotificacoesGateway (socket.io) entrega
  em tempo real: cada socket autenticado entra na sala pessoal user:<id> e recebe
  o evento "notificacao:nova".
- Endpoints: GET /notificacoes, GET /notificacoes/contador, PATCH :id/lida,
  PATCH /notificacoes/lidas, GET/PATCH /notificacoes/preferencias.

DISPAROS integrados aos fluxos (respeitando preferências):
- proposta recebida -> cliente; proposta aceita -> prestador;
- nova mensagem -> demais participantes da conversa;
- contrato assinado/ativado -> outra parte; marco entregue -> cliente;
  marco aprovado / contrato concluído -> prestador;
- indicação reservada -> indicador; denúncia resolvida -> denunciante.

FRONTEND:
- Sino (NotificationBell) no menu com contador de não lidas e atualização em TEMPO
  REAL via socket; dropdown com itens, clique marca lida e navega pelo link.
- Página /notificacoes (lista + "marcar todas" + preferências por categoria com toggles).

BANCO: migração 20260630200317_parte_xiii_notificacoes.

VERIFICAÇÃO:
- e2e: 34/34 PASSARAM (12 fluxo + 12 fases + 6 contratos + 4 notificações). Os testes de
  notificação cobrem: disparo em proposta recebida/aceita, contador, marcar todas lidas
  e ENFORCEMENT de preferência (categoria desligada não gera novas notificações).
- API DI OK no boot (7 rotas de notificação mapeadas). Smoke ao vivo: proposta real ->
  contador do cliente = 1, última = "Nova proposta recebida" (proposta.recebida).
  Páginas /notificacoes e / -> HTTP 200.
==================================================================

------------------------------------------------------------------
2026-06-30 — Sessão 5: Parte XV — Dados, BI e inteligência operacional
------------------------------------------------------------------

BACKEND (módulo "bi", somente MODERADOR/ADMIN via PapeisGuard, SEM migração):
- Agregações read-only sobre os dados já existentes (incl. trilha de auditoria).
- GET /admin/bi/overview: KPIs de liquidez (taxa com resposta, propostas/solicitação),
  conversão (taxa de aceite, contratos, taxa de conclusão), confiança (avaliações,
  nota média, denúncias) e conhecimento (insights).
- GET /admin/bi/funil: Solicitações -> Com proposta -> Aceita -> Contrato -> Concluído (com %).
- GET /admin/bi/categorias: distribuição de solicitações por categoria (groupBy).
- GET /admin/bi/serie?dias=N: série temporal de eventos por dia ($queryRaw date_trunc
  sobre audit_logs).

FRONTEND:
- Página /admin/bi: cards de KPI, funil (barras), categorias (barras) e série temporal
  (mini gráfico de barras). Tudo em CSS puro, sem dependência de biblioteca de gráficos.
  Botão "Dados & BI" no painel /admin.

VERIFICAÇÃO:
- e2e: 38/38 PASSARAM (12 fluxo + 12 fases + 6 contratos + 4 notificações + 4 BI). Os de BI
  cobrem bloqueio de não-staff (403) e o formato de overview/funil/categorias/série.
- Smoke ao vivo (admin): liquidez 23 solicitações / 95,7% com resposta; conversão 86,4%
  de aceite, 6 contratos; confiança média 4,7★; 196 eventos auditados; funil
  23->22->19->6->5. Páginas /admin/bi e / -> HTTP 200.
==================================================================

------------------------------------------------------------------
2026-07-01 — Sessão 6: Parte XX — Monetização opcional (sem destruir gratuidade)
------------------------------------------------------------------

PRINCÍPIO (decisão fundadora): o NÚCLEO é e continua GRÁTIS (publicar, propor,
conversar, indicar, avaliar). Planos são camadas OPCIONAIS e nunca condicionam a
participação. Pagamento é SIMULADO/registrado — não movimenta dinheiro real.

BACKEND (módulo "monetizacao"):
- Modelos: Plano (catálogo: codigo, nome, preço, periodicidade, tipo, recursos[],
  destaque) e Assinatura (status, metodo="simulado", inicio/fim).
- Endpoints: GET /planos (público), GET /planos/minhas, POST /planos/:id/assinar
  (simulado; bloqueia duplicada ativa; plano com recurso "selo_verificado" concede
  verificado=true), POST /assinaturas/:id/cancelar. Tudo auditado.

FRONTEND:
- Página /planos: banner "núcleo é grátis", cards do catálogo (recursos internos
  como selo_verificado ficam ocultos), assinar/cancelar, "minhas assinaturas".
  Link "Planos" no menu.

SEED: catálogo com 4 planos — Comunidade (R$0), Verificado Pro (R$29,90/mês, selo),
Empresarial (R$99,90/mês), Patrocínio rotulado (R$199,90 único).

VERIFICAÇÃO:
- e2e: 45/45 PASSARAM (12 fluxo + 12 fases + 6 contratos + 4 notif + 4 BI + 7 monetização).
  Cobre: catálogo público tem plano grátis, NÚCLEO funciona sem assinatura, assinar
  simulado (ATIVA/metodo=simulado), duplicada=409, cancelar, exige auth=401.
- Smoke ao vivo: catálogo Comunidade/Verificado Pro/Empresarial/Patrocínio;
  assinatura "Verificado Pro" -> ATIVA/simulado. Páginas /planos e / -> HTTP 200.
==================================================================

------------------------------------------------------------------
2026-07-01 — Sessão 7: Preparação de deploy (colocar na web)
------------------------------------------------------------------

CONTEXTO: usuário pediu para "subir no web". Honestidade registrada: publicar
exige contas/hospedagem no nome do usuário (não é possível eu criar conta/pagar/
autenticar por ele). Escolha de hospedagem delegada a mim: RENDER (site+API+banco
num provedor só, deploy a partir do GitHub); usuário já tem GitHub.

FEITO (parte que é minha):
- git init + 2 commits (132+ arquivos). Confirmado que node_modules/.pgdata/.env
  NÃO entram no repositório.
- render.yaml (Blueprint): Postgres + serviço API + serviço Web, com variáveis
  já ligadas (DATABASE_URL do banco, JWT_SECRET gerado, WEB_ORIGIN e
  NEXT_PUBLIC_API_URL apontando para os domínios .onrender.com). Start da API roda
  migrate deploy + seed (idempotente).
- engines Node>=20 nos dois package.json.
- DEPLOY.md: passo a passo (push pro GitHub -> New Blueprint no Render -> Apply).
  Documentados limites do grátis (serviços dormem; Postgres grátis expira) e
  alternativa Railway (sempre ligado, ~US$5/mês).
- VERIFICAÇÃO: `next build` de produção PASSOU (14/14 páginas, exit 0); API já
  buildava limpo. WebSocket (chat/notificações) compatível com Render.

FALTA (parte do usuário, precisa das contas dele):
1) criar repo no GitHub e `git push`; 2) Render -> New Blueprint -> escolher o
   repo -> Apply. Depois: site em https://alpha-hub-web.onrender.com.
==================================================================

------------------------------------------------------------------
2026-07-01 — Sessão 8: NO AR (deploy concluído no Render)
------------------------------------------------------------------

PUBLICADO com sucesso:
- Repositório: https://github.com/AlphaControlPE/alpha-hub (push feito pelo usuário).
- Render Blueprint aplicado: banco + API + Web criados a partir do render.yaml.

URLS PÚBLICAS (LIVE):
- Site: https://alpha-hub-web.onrender.com
- API:  https://alpha-hub-api.onrender.com/api  (health OK)

VERIFICAÇÃO EM PRODUÇÃO (via curl):
- GET /api/planos -> catálogo (Comunidade/Verificado Pro/...) => migração + seed OK no Postgres da nuvem.
- POST /api/auth/login (admin@alphahub.dev) -> JWT OK => auth + usuários do seed OK.
- GET /api/solicitacoes -> dados retornando => banco populado.
- Web respondeu HTTP 200.

OBSERVAÇÕES:
- Plano grátis do Render: serviços "dormem" após ~15 min sem uso; 1º acesso depois
  demora ~30-60s pra acordar (visto na prática: API respondeu 000 e depois 200).
- Postgres grátis do Render expira com o tempo (migrar p/ plano pago quando for pra valer).
- Pagamentos seguem SIMULADOS (sem dinheiro real) até integrar parceiro.
==================================================================

------------------------------------------------------------------
2026-07-01 — Sessão 9: Parte IV — Perfis públicos & Portfólio (agente + revisão)
------------------------------------------------------------------

PROCESSO (a pedido do usuário): "cada agente para uma etapa, depois revise".
Agente 1 implementou o backend (módulo perfil, schema, app.module, tipos web) e
foi interrompido por limite de sessão; o main REVISOU (aprovado: padrões de
auditoria/guards/validação corretos) e COMPLETOU frontend, links e teste e2e.

BACKEND (módulo "perfil"):
- Models: PortfolioItem (titulo, descricao, imagemUrl?, link?) e ServicoOferecido
  (titulo, descricao, precoBase? centavos), ambos por usuário, cascade delete.
- Endpoints: GET /usuarios/:id/perfil (público: bio, selo, portfólio, serviços;
  sem vazar email/senha), GET /perfil/me, POST/DELETE /perfil/portfolio[/:id],
  POST/DELETE /perfil/servicos[/:id] (só dono; auditado).

FRONTEND:
- /usuarios/[id]: perfil público com avatar, selo "✓ verificado", bio, reputação
  multidimensional em barras (reusa GET /usuarios/:id/reputacao da Parte X),
  portfólio e serviços (preço "a partir de"/"a combinar").
- /perfil: edição (adicionar/remover casos e serviços) + link p/ perfil público.
- Links: avatar do menu -> /perfil; nome do prestador na proposta e autor da
  solicitação -> /usuarios/{id}.

BANCO: migração 20260701204010_parte_iv_perfil aplicada.

VERIFICAÇÃO: build API + typecheck web OK; e2e 52/52 PASSARAM (7 suítes; novo
perfil.e2e-spec com 7 testes: perfil público sem auth e sem vazar dados, dono
edita, terceiro não remove=403, validação=400).
==================================================================

------------------------------------------------------------------
2026-07-01 — PAUSA por ordem do usuário ("pare até segunda ordem")
------------------------------------------------------------------
ESTADO ao pausar:
- NO AR (Render, intocado): site https://alpha-hub-web.onrender.com e
  API https://alpha-hub-api.onrender.com/api — com MVP + Partes III..XX até a
  Parte IV inclusive (commit 6a8bc20 publicado).
- Parte III (Organizações/verificação): código do Agente 2 PRONTO NO WORKING TREE,
  NÃO commitado / NÃO revisado / NÃO migrado / NÃO publicado. Inclui: enum
  StatusVerificacao + campos na Organization (schema), módulo organizacoes/ na API,
  tipos web e (a conferir) páginas/nav/teste.
- Local: nenhum serviço rodando (3000/3001/5432 fechados). Dados locais preservados
  em .pgdata.
RETOMADA: revisar diff do working tree -> prisma generate+migrate -> build/typecheck
-> e2e -> completar frontend se faltar -> commit+push. Depois: Agente 3 (XVIII/XIX)
e etapa de domínio.
==================================================================

------------------------------------------------------------------
2026-07-05 — Sessão 10: Parte III integrada + P0 (blindar produção)
------------------------------------------------------------------

PARTE III (finalizada/publicada): frontend /organizacoes (gestão de equipe:
membros por email, papéis DONO/ADMIN/MEMBRO, sair/remover, pedir verificação),
seção "Verificações pendentes" no /admin, link no menu, e2e (9 testes). Migração
parte_iii_organizacoes. Commit 7424b3d.

P0 — BLINDAGEM DE SEGURANÇA:
- Credenciais de demonstração REMOVIDAS da tela de login (não anunciar a visitantes).
- Senha do admin via env: seed usa ADMIN_SENHA (e ADMIN_EMAIL); sem env cai na
  senha de demo. render.yaml agora pede ADMIN_SENHA/ADMIN_EMAIL (sync:false) —
  AÇÃO DO USUÁRIO: definir ADMIN_SENHA forte no painel do Render.
- Rate-limiting (@nestjs/throttler): global 120/min; login e register 10/min por IP.
  Desativado em teste (skipIf NODE_ENV=test). trust proxy p/ IP real no Render.
- Login bloqueado (403) para usuário com sanção ativa SUSPENSAO/BANIMENTO.
- helmet: cabeçalhos de segurança (X-Frame-Options, X-Content-Type-Options...).

NOTA DE AMBIENTE: rodar `npm install` dentro de apps/api (workspace) hasteou o
node_modules e quebrou a resolução do class-validator localmente. Corrigido
reinstalando apps/api auto-contido (escondendo o package.json raiz no install).
Não afeta o Render (install limpo por serviço).

VERIFICAÇÃO: build API + typecheck web OK. e2e 64/64 (novo seguranca.e2e-spec:
login banido=403). Smoke no build de produção: helmet headers presentes;
rate-limit no login retorna 429 após 10 tentativas.
==================================================================

------------------------------------------------------------------
2026-07-05 — Sessão: P0 (feito por sessão anterior) + P1 backup + nota de cwd
------------------------------------------------------------------
- Confirmado: P0 de segurança JÁ commitado e PUBLICADO (94372aa): rate-limit
  (@nestjs/throttler), login de usuário sancionado bloqueado, helmet, senha admin
  via env (ADMIN_SENHA/ADMIN_EMAIL no render.yaml, sync:false). Site no ar protegido.
- P1 Preservação (backup): scripts/backup-db.sh|.ps1 (dump comprimido), restore-db.sh,
  .github/workflows/backup.yml (Action diária, secret DATABASE_URL), BACKUP.md
  (External URL do Render, backup manual/automático, restore, plano p/ expiração:
  upgrade pago OU migrar p/ Neon). backups/ no .gitignore.
- Agentes em background NÃO sobrevivem a reinício de sessão (Agente A morreu deixando
  só 2 scripts; completei o P1 direto). Decisão: nesta sessão instável, implementar
  direto em vez de depender de subagentes.
- NOTA cwd: a sessão abre em INV - V1, mas o Alpha Hub é em C:\Users\alpha\Desktop\Alpha-Hub
  (sempre por caminho absoluto). Registrado na memória (alpha-hub-cwd-vinculo).
  Para resolver de vez: abrir o Claude Code já dentro da pasta Alpha-Hub.
==================================================================

==================================================================
2026-07-06 — HANDOFF (fim de sessão; usuário vai abrir novo chat)
==================================================================
COMO CONTINUAR: abrir o Claude Code JÁ DENTRO de C:\Users\alpha\Desktop\Alpha-Hub
(assim git/worktrees/agentes funcionam direto e a memória do projeto é a certa).
A memória do Alpha Hub foi MOVIDA para a pasta de projeto correta
(.claude/projects/C--Users-alpha-Desktop-Alpha-Hub/memory/).

ESTADO ATUAL (tudo no ar, verificado):
- Site:  https://alpha-hub-web.onrender.com   |  API: https://alpha-hub-api.onrender.com/api
- Repo:  https://github.com/AlphaControlPE/alpha-hub  (branch main; git push => Render redeploya)
- Partes implementadas: MVP + III, IV, VII, IX, X, XI, XII, XIII, XIV, XV, XX. 61/61 e2e.
- P0 SEGURANÇA (live, commit 94372aa): rate-limit (throttler), login de banido bloqueado,
  helmet, senha admin via env (ADMIN_SENHA no render.yaml, sync:false), credenciais demo
  removidas da tela de login.
- P1 BACKUP (live, 49b0b2d): scripts/backup-db.sh|.ps1 + restore-db.sh, GitHub Action diária
  (.github/workflows/backup.yml — precisa do secret DATABASE_URL no GitHub), BACKUP.md com
  plano p/ expiração do Postgres grátis (upgrade Render OU migrar p/ Neon).
- P2 LEGAL (live, 32c13aa): páginas /termos e /privacidade (LGPD) + rodapé.

AÇÕES DO USUÁRIO PENDENTES (fecham riscos/destravam features):
1. Render: definir env ADMIN_SENHA (senha forte) no serviço alpha-hub-api => fecha o admin.
2. GitHub: criar secret DATABASE_URL (External Database URL do Render) => liga o backup automático.
3. Antes do Postgres grátis expirar: seguir BACKUP.md (upgrade ou Neon).

A FAZER (próximo chat):
- "Esqueci a senha": PRECISA de provedor de e-mail (sugestão Resend, grátis) + API key do
  usuário. NÃO shipar de fachada. Depois da chave: fluxo esqueci->email->redefinir completo.
- PWA + acessibilidade (P3): manifest/ícones/instalável + aria/foco/skip-link + SEO/OpenGraph.
- Validar com usuários reais (recomendação do escopo antes de expandir).
==================================================================

------------------------------------------------------------------
2026-07-06 — Parte XVIII/XIX: PWA + acessibilidade + SEO (Agente C + revisão)
------------------------------------------------------------------
Trabalho do Agente C (feito 05/07, estava no working tree sem commitar); revisado
e publicado agora.
- PWA instalável: src/app/manifest.ts (/manifest.webmanifest), ícones PNG 192/512 +
  SVG maskable/any + apple-icon.png em apps/web/public. display standalone, theme #0b1020.
- SEO: metadata completa no layout (title template, description, keywords, canonical,
  OpenGraph + Twitter card com og.png), viewport com themeColor/colorScheme, robots,
  src/app/sitemap.ts (/sitemap.xml, só rotas públicas), src/lib/site.ts (SITE_URL
  sobrescrevível por NEXT_PUBLIC_SITE_URL).
- Acessibilidade: skip-link "Pular para o conteúdo", <main id=conteudo tabIndex=-1>,
  <nav aria-label>, aria-label/expanded no sino de notificações, ícones aria-hidden,
  :focus-visible e .sr-only no globals.css.
VERIFICAÇÃO: typecheck web OK (após build regenerar .next; erros iniciais eram cache
velho das rotas geradoras de ícone, já removidas) e `next build` de produção OK
(14 rotas + /manifest.webmanifest + /sitemap.xml).
==================================================================

------------------------------------------------------------------
2026-07-06 — Sessão: reconciliação + verificação do P3 (já estava live)
------------------------------------------------------------------
CONTEXTO: retomei a sessão para "começar pelo mais coerente" e escolhi o P3
(PWA + acessibilidade + SEO). Ao final descobri, pelo git, que o P3 JÁ ESTAVA
COMMITADO em 4300b91 e no origin/main — portanto JÁ PUBLICADO no Render. Reimplementei
por engano (os PNGs saíram byte-idênticos por serem gerados do mesmo SVG da marca, então
`git status` não acusou diff em nenhum arquivo do P3).

VALOR REAL DESTA SESSÃO (validação honesta de que o P3 funciona mesmo):
- `next build` de produção: VERDE (20 rotas; /manifest.webmanifest e /sitemap.xml estáticos).
- `next start` (produção local): og:image e canonical resolvem para
  https://alpha-hub-web.onrender.com (o localhost visto em `next dev` é só quirk do modo dev).
- Todos os assets 200 com content-type correto: /og.png, /apple-icon.png, /icon-192.png,
  /icon-512.png, /icon.svg, /icon-maskable.svg, /sitemap.xml, /robots.txt.
- Acessibilidade conferida no DOM: skip-link "Pular para o conteúdo", <main id=conteudo
  tabindex=-1>, <nav aria-label="Navegação principal">, aria no sino/avatar.
- LIVE no Render conferido via curl: /manifest.webmanifest responde e og:image aponta
  para a URL de produção. => P3 CONFIRMADO no ar.

MUDANÇA NOVA (única com diff): render.yaml ganhou NEXT_PUBLIC_SITE_URL=
https://alpha-hub-web.onrender.com no serviço web — torna a URL canônica explícita
(metadataBase/SEO/sitemap/canonical) em vez de depender só do fallback no código.

NOTA/APRENDIZADO: sempre conferir `git status`/`git log` do arquivo ANTES de reimplementar
uma "pendência" do handoff — o handoff (P3 a fazer) estava desatualizado; o P3 já fora
entregue em 4300b91.
==================================================================

------------------------------------------------------------------
2026-07-06 — Sessão 11: SEO por página + JSON-LD + sitemap dinâmico
------------------------------------------------------------------
DECISÃO (escolha do usuário entre 4 opções): estender o SEO já validado com
metadata única POR PÁGINA + dados estruturados schema.org (maior descoberta e
compartilhamento das páginas públicas, principalmente as dinâmicas).

IMPLEMENTADO (frontend Next.js):
- src/lib/seo.ts: buildMetadata() (canonical+OpenGraph+Twitter consistentes,
  og:image padrão /og.png reafirmado — o merge do Next SUBSTITUI o objeto
  openGraph inteiro, não mescla), truncate() p/ descriptions, fetchPublic()
  (fetch server-side tolerante a falha, revalidate 600s, nunca lança).
- Metadata por página via layout.tsx (páginas são 'use client', não exportam
  generateMetadata):
  · Estáticas: /planos, /login, /comunidade (+ /termos e /privacidade enriquecidas).
  · Dinâmicas com generateMetadata buscando a entidade real na API:
    /solicitacoes/[id] (título+descrição da solicitação), /usuarios/[id]
    (nome+bio do perfil), /comunidade/[id] (título+conteúdo do insight).
  · Privadas (8): admin, perfil, notificacoes, conversas, contratos, indicacoes,
    organizacoes, nova => title de aba + robots noindex,nofollow.
- JSON-LD (schema.org): raiz com Organization + WebSite + SearchAction
  (target /?q={search_term_string}); /usuarios/[id] com ProfilePage + Person
  (knowsAbout dos serviços).
- Busca deep-linkável na home: lê ?q=/?categoria= ao montar e sincroniza a URL
  (history.replaceState) — torna o SearchAction verdadeiro e buscas compartilháveis.
- Sitemap dinâmico: além das 6 rotas estáticas, inclui solicitações (limit=100,
  priority .8) e insights (priority .5) buscados da API; se a API estiver fora,
  degrada para as estáticas sem falhar (comprovado na prática durante a sessão).
- render.yaml: NEXT_PUBLIC_SITE_URL explícito no serviço web (canonical/metadataBase).

BUGS ENCONTRADOS E CORRIGIDOS DURANTE A VERIFICAÇÃO:
- og:image sumia nas páginas com openGraph próprio (merge substitui o objeto) =>
  buildMetadata reafirma images:['/og.png'] por padrão.
- /comunidade/[id] perdia o sufixo "· Alpha Hub" => layout intermediário de
  /comunidade passou a declarar title como objeto {default, template} (string
  simples zera o template herdado da raiz).

VERIFICAÇÃO (dev + build de produção, com Postgres portátil + API locais):
- Títulos/canonicals únicos conferidos por curl em 8 páginas, incluindo entidades
  reais ("Preciso de uma logo para minha startup · Alpha Hub" etc.).
- JSON-LD presente: Organization/WebSite/SearchAction na raiz; ProfilePage/Person
  no perfil. noindex nas 8 privadas; index,follow nas públicas.
- /?q=logo semeia o campo de busca e filtra (15 resultados no seed local).
- Sitemap com 63 URLs (6 estáticas + dinâmicas) e URLs de produção.
- `next build` de produção VERDE (20 rotas).
NOTA DE AMBIENTE: rodar `next build` com o `next dev` de pé derruba o dev server
(compartilham .next) — reiniciar o dev depois do build.
==================================================================

------------------------------------------------------------------
2026-07-06 — Sessão 12: PWA offline (service worker + página offline)
------------------------------------------------------------------
DECISÃO: completar a história de PWA (site já era instalável desde 4300b91) com
funcionamento offline. Estratégias escolhidas para NUNCA servir dado velho:
- Páginas (navegação): network-first -> cache -> /offline (cache só sem rede).
- Assets estáticos (/_next/static, imagens, fontes): stale-while-revalidate
  (são hashados; cache é seguro e acelera).
- API/WebSocket/cross-origin: NUNCA interceptados (auth, tempo real, CORS).

IMPLEMENTADO:
- apps/web/public/sw.js: service worker versionado (ah-v1; trocar VERSION invalida
  tudo no próximo deploy), precache do CORE (/offline, manifest, 3 ícones),
  limpeza de caches antigos no activate, skipWaiting+clients.claim.
- /offline (src/app/offline/page.tsx): fallback com marca, estilos INLINE de
  propósito (renderiza bem mesmo sem o CSS/JS do app em cache), noindex.
- SwRegister.tsx no layout raiz: registra /sw.js SÓ em produção (em dev o cache
  atrapalharia o HMR); falha silenciosa.
- .claude/launch.json: nova config "web-prod" (next start) p/ testar produção local.

VERIFICAÇÃO (dupla):
1. AO VIVO (build de produção + next start): SW registrado/ativado/controlando a
   página; precache com os 5 itens; navegar para /planos populou o runtime cache
   (HTML + 9 chunks JS/CSS). Página /offline responde com o conteúdo esperado.
2. COMPORTAMENTAL (Node + vm, contra o sw.js REAL shipado): 10/10 PASSARAM —
   install precacheia; activate limpa versão antiga; rede OK usa rede e cacheia;
   OFFLINE serve página visitada do cache; OFFLINE cai no /offline p/ página nunca
   vista; GET comum sem cache -> 503; cross-origin (API) passa direto; asset
   cacheado servido offline; POST nunca interceptado.
   (Motivo do teste em Node: matar o servidor local derruba junto a ponte de
   avaliação do preview, e o Chrome extension falhou ao criar aba — impossível
   simular offline de verdade no browser da sessão.)

LIMITAÇÃO CONHECIDA: o SW só se registra em produção (Render). Em localhost dev
não há SW — comportamento intencional.
==================================================================

------------------------------------------------------------------
2026-07-06 — Sessão 13: Polimento de UX (404/erro, skeletons, estados vazios)
------------------------------------------------------------------
(1ª metade do "os dois" autorizado pelo usuário; 2ª metade = esqueci a senha.)

IMPLEMENTADO (frontend):
- not-found.tsx: 404 com marca ("404" em gradiente, CTAs p/ home e comunidade),
  dentro do layout (nav/rodapé preservados).
- error.tsx: error boundary com marca, botão "Tentar de novo" (reset) + volta à
  home; mostra código digest quando existir.
- Skeletons (shimmer): classe .skeleton no globals.css (respeita
  prefers-reduced-motion via kill switch já existente) + componentes Skeleton/
  SkeletonList (role=status p/ leitores de tela). Aplicados: home (lista),
  /solicitacoes/[id] (detalhe), /conversas, /comunidade.
- BUG DE UX CORRIGIDO de quebra: /conversas e /comunidade mostravam o estado
  VAZIO enquanto carregavam (mentira momentânea); agora têm estado buscando.
- Estados vazios com ícone + explicação + CTA: home (busca vs 1ª publicação,
  logado vs visitante), conversas (→ ver solicitações), comunidade (→ publicar
  1º insight), notificações ("Tudo em dia"), contratos (→ ver solicitações),
  indicações.

VERIFICAÇÃO: `next build` produção VERDE (21 rotas). Em produção local
(next start): 404 renderiza completa (snapshot de acessibilidade: heading,
CTAs, nav, rodapé); estado vazio da home confirmado (variante visitante com
"Criar conta grátis"). Skeletons são caminho trivial (carregando ? skeleton).

NOTA IMPORTANTE DE AMBIENTE: no fim da sessão a porta 3001 estava ocupada por
OUTRO sistema (resposta com envelope success/data/error/meta = Recy/Alpha
Control?). NÃO foi morto — não é do Alpha Hub. Se for rodar a API local do
Alpha Hub, liberar a 3001 antes (ou conferir quem é o processo).
==================================================================

------------------------------------------------------------------
2026-07-07 — Sessão 14: "Esqueci a senha" REAL (gated por RESEND_API_KEY)
------------------------------------------------------------------
(2ª metade do "os dois". Decisão antiga respeitada: NÃO shipar de fachada —
o recurso fica DESLIGADO e invisível até existir chave de e-mail; nada finge
funcionar.)

BACKEND:
- Migração 20260707100237_esqueci_senha: model TokenRedefinicaoSenha
  (tokenHash sha-256 ÚNICO — o token cru vai só no e-mail; expiraEm 1h;
  usadoEm; cascade delete com o usuário).
- common/email/email.service.ts: envio via API do Resend com fetch nativo
  (sem SDK). `disponivel` = existe RESEND_API_KEY. Em NODE_ENV=test não sai
  nada pra rede (último e-mail inspecionável pelos testes). Remetente:
  RESEND_FROM ou "Alpha Hub <onboarding@resend.dev>".
- AuthService: solicitarRedefinicao (503 se desligado; resposta SEMPRE genérica
  — anti-enumeração de e-mail; token randomBytes(32); pedido novo invalida
  anteriores; falha de envio não revela conta e é auditada) e redefinirSenha
  (valida não-usado+não-expirado, troca senha, marca usado e revoga pendentes
  em transação). Tudo auditado.
- Endpoints: GET /auth/recursos (o front só mostra o que funciona),
  POST /auth/esqueci (throttle 5/min — mais rígido que login),
  POST /auth/redefinir (10/min).

FRONTEND:
- /login: link "Esqueci minha senha" SÓ aparece se /auth/recursos disser que
  o recurso está ligado.
- /esqueci: form de e-mail -> mensagem genérica (mesma para qualquer e-mail);
  503 vira aviso honesto de recurso desabilitado.
- /redefinir?token=: nova senha + confirmação (useSearchParams com Suspense);
  sem token -> CTA para pedir novo link. Ambas noindex.

VERIFICAÇÃO:
- e2e: 74/74 PASSARAM (10 suítes; novo esqueci-senha.e2e-spec com 10 testes:
  gate 503 sem chave, recursos liga/desliga, anti-enumeração (e-mail
  desconhecido não envia nada), fluxo completo com token extraído do e-mail
  simulado, senha nova funciona/antiga falha, uso único, token lixo=400,
  senha curta=400, token expirado=400).
- Build API (nest) e web (next, 23 rotas) VERDES.
- CORREÇÃO no próprio teste: token expirado usava valor fixo e colidia com
  runs anteriores (unique tokenHash) — trocado por aleatório.

ATIVAÇÃO (ação do usuário, quando quiser ligar):
1. Criar conta grátis em https://resend.com -> API Keys -> criar chave.
2. Render -> serviço alpha-hub-api -> Environment -> RESEND_API_KEY = chave.
3. Pronto: o link "Esqueci minha senha" passa a aparecer no login sozinho.
(Opcional: verificar domínio próprio no Resend e setar RESEND_FROM.)
==================================================================

------------------------------------------------------------------
2026-07-07 — Sessão 15: Pagamento REAL de planos — Mercado Pago (Pix/cartão)
------------------------------------------------------------------
PEDIDO DO USUÁRIO: gateway de pagamento Pix/cartão; escolheu Mercado Pago.
LIMITE HONESTO: NÃO posso criar a conta MP nem gerar chaves (dados financeiros
são do usuário). Padrão Resend repetido: integração completa GATED por
MP_ACCESS_TOKEN — sem a chave, o checkout fica desligado e o modo simulado
continua para demonstração. ESCOPO v1: pagamento real só dos PLANOS (receita
da plataforma). O escrow dos CONTRATOS segue SIMULADO — split marketplace no
MP exige credenciamento próprio (fica como camada futura, fiel ao escopo).

BACKEND:
- Migração 20260707102012_pagamento_mercadopago: StatusAssinatura ganha
  PENDENTE; Assinatura ganha preferenciaId e pagamentoExternoId (idempotência).
- common/pagamentos/mercadopago.service.ts: Checkout Pro via REST (fetch, sem
  SDK). criarPreferencia (item BRL, external_reference=assinatura.id,
  back_urls /planos?pagamento=..., notification_url API_PUBLIC_URL/api/webhooks/
  mercadopago, statement_descriptor ALPHA HUB) e buscarPagamento. Em
  NODE_ENV=test nada sai pra rede (preference e pagamentos simulados em memória).
- MonetizacaoService: GET /planos/recursos {pagamentoReal}; POST /planos/:id/
  checkout (503 sem chave; 400 p/ plano grátis; 409 se já ativa; cria assinatura
  PENDENTE e invalida pendentes anteriores; devolve URL do checkout); ANTI-BYPASS:
  com MP ligado, plano pago NÃO pode ser ativado pelo caminho simulado (400).
- Webhook POST /webhooks/mercadopago: NUNCA confia no corpo — busca o pagamento
  na API do MP com a nossa credencial; valida external_reference e VALOR
  (>= preço do plano); ativa (fimEm +30d se MENSAL; concede selo verificado se
  o plano tiver o recurso); idempotente; responde sempre 2xx pro MP não
  re-tentar à toa. Valor divergente é auditado e NÃO ativa.

FRONTEND (/planos):
- Botão vira "Assinar com Pix/cartão" quando pagamentoReal (redireciona ao MP);
  texto do hero muda de "simulado" para "processado pelo Mercado Pago".
- Retorno do checkout (?pagamento=sucesso|pendente|falha) vira banner.
- "Minhas assinaturas": badge "aguardando pagamento" (PENDENTE) + método.

VERIFICAÇÃO: e2e 84/84 (11 suítes; novo pagamentos.e2e-spec com 10 testes:
gate 503, demo preservada sem chave, anti-bypass 400 com chave, checkout grátis
400, checkout cria PENDENTE + preference com valor certo, webhook valor
divergente NÃO ativa, aprovado ativa, idempotente, desconhecido sem ação,
409 se já ativa). Builds API e web VERDES.

ATIVAÇÃO (ação do usuário, quando quiser ligar):
1. Conta em https://mercadopago.com.br/developers -> Suas integrações ->
   Criar aplicação (Checkout Pro).
2. Copiar o Access Token de TESTE -> Render -> alpha-hub-api -> Environment ->
   MP_ACCESS_TOKEN. Testar um pagamento com cartão de teste do MP.
3. Quando validar: trocar pelo Access Token de PRODUÇÃO. Pronto — o botão
   "Assinar com Pix/cartão" aparece sozinho em /planos.

ADENDO (mesma data): usuário criou a aplicação MP (app 5543586586784930) e um
USUÁRIO DE TESTE, e forneceu as credenciais no chat. VALIDADO por mim direto
na API do MP (sem tocar em painel):
- GET /users/me com o Access Token -> conta de TESTE confirmada
  (tipo_conta_teste=true, TESTUSER..., site MLB) — sandbox, sem dinheiro real.
- POST /checkout/preferences com o payload exato do nosso service ->
  PREFERENCE CRIADA (init_point válido, BRL) => token + formato aprovados.
SEGREDOS NÃO gravados em repo/arquivos (uso efêmero; ficam só no painel do
Render, colados pelo usuário). Higiene: senha/código do usuário de teste são
só para o usuário logar no sandbox; recomendada rotação das chaves quando
migrar para produção.
PENDENTE (usuário): colar MP_ACCESS_TOKEN no Render -> eu verifico o live
(GET /planos/recursos deve virar pagamentoReal=true e o botão Pix/cartão
aparecer em /planos).
==================================================================

------------------------------------------------------------------
2026-07-07 — Sessão 16: INCIDENTE — deploys da API falhando desde sábado (FIX)
------------------------------------------------------------------
SINTOMA: todo deploy da API no Render falhava com "Exited with status 1"
desde o commit 94372aa (P0, sábado). O Render mantinha a VERSÃO ANTIGA no ar
(por isso o site parecia normal). Web deployava OK. Migrações novas APLICARAM
no banco de produção (esqueci_senha e pagamento_mercadopago) — só o app não
subia. Erro real (logs colados pelo usuário): NestJS não achava
@nestjs/platform-express em runtime.

DIAGNÓSTICO (com reproduções locais em pasta limpa):
- O campo "workspaces" no package.json da RAIZ fazia o npm (no Render, rodando
  dentro de apps/api) detectar o monorepo e instalar com HOISTING guiado pelo
  package-lock.json da raiz — que foi REESCRITO de forma inconsistente na
  sessão do P0 (o incidente de hoisting registrado na Sessão 10). Resultado no
  ambiente do Render: node_modules sem @nestjs/platform-express (que só é
  carregado em runtime — o build passa, o app morre ao subir).
- "Clear build cache & deploy" (tentado pelo usuário) NÃO resolveu.
- Falsas pistas descartadas: package.json/locks dos apps estavam corretos;
  instalação limpa local funcionava (npm 11); o problema era a combinação
  workspaces + lock raiz inconsistente no npm do Render.

FIX (determinístico, sem depender de versão de npm):
- REMOVIDO "workspaces" do package.json da raiz (nada usava — os scripts são
  todos npm --prefix) e DELETADO o package-lock.json da raiz (vestigial).
- Cada app agora instala 100% auto-contido a partir do próprio lockfile.
ENSAIO COMPLETO em cópia limpa (git archive + mudanças) ANTES do push:
- API: npm install (sem hoisting, platform-express no lugar) -> prisma
  generate -> nest build -> BOOT ok -> /api/health, /api/planos/recursos e
  /api/auth/recursos respondendo.
- WEB: npm install auto-contido ok -> next build ok (23 rotas).
LIÇÃO: em monorepo com deploy por rootDir, ou se usa workspaces DE VERDADE
(lock raiz como fonte única, deploy instalando na raiz), ou não se usa nada —
estado híbrido quebra de formas silenciosas.

RESOLVIDO (commit 018ddd4, verificado no live em 2026-07-08 02:37 UTC):
- /api/health OK; /api/auth/recursos e /api/planos/recursos respondendo
  (=> build novo no ar, com esqueci-senha e Mercado Pago inclusos);
  POST /api/webhooks/mercadopago existe (201).
- esqueciSenha=false e pagamentoReal=false: CORRETO — as chaves
  (RESEND_API_KEY / MP_ACCESS_TOKEN) ainda não foram coladas no painel.
ÚNICO PASSO RESTANTE (usuário): Render -> alpha-hub-api -> Environment ->
adicionar MP_ACCESS_TOKEN (produção MP já validada por mim na API do MP).
==================================================================

------------------------------------------------------------------
2026-07-08 — Sessão 17: novo tema visual "Editorial sério" (fornecido pelo usuário)
------------------------------------------------------------------
O usuário entregou um globals.css completo (tema "Editorial sério", direção 3b):
fundo papel (#f1f1ef), tinta (#1a1c20), verde editorial (#166a45), cantos RETOS
(radius 0), sem sombras, tipografia Newsreader (títulos, serif) + Instrument
Sans (interface) via Google Fonts. Mesma API de classes — componentes intactos.

APLICADO + PASSE DE COERÊNCIA (para o resto acompanhar o tema):
- apps/web/src/app/globals.css substituído pelo arquivo do usuário.
- layout.tsx: viewport themeColor #0b1020 -> #fcfcfb; colorScheme dark -> light.
- manifest.ts (PWA): background_color #f1f1ef, theme_color #fcfcfb.
- /offline reescrita na paleta editorial (inline styles de propósito, como antes).
MANTIDOS por decisão de escopo (identidade de marca ≠ tema do site): ícones
PWA/favicon (A em gradiente azul-verde) e og.png (card social escuro).
Se o usuário quiser, regenerar depois no estilo editorial.

VERIFICAÇÃO: next build VERDE (23 rotas). Estilos computados conferidos ao
vivo no build de produção: body #f1f1ef/Instrument Sans, h1 Newsreader 34px,
.btn-primary tinta->papel. (Screenshot indisponível — preview instável.)
==================================================================

==================================================================
2026-07-08 — HANDOFF (usuário vai abrir novo chat p/ aliviar contexto)
==================================================================
COMO CONTINUAR: abrir o Claude Code JÁ DENTRO de C:\Users\alpha\Desktop\Alpha-Hub.
A memória do projeto (atualizada nesta sessão) traz o estado, as pendências e os
próximos passos — ler primeiro.

ESTADO: tudo no ar e verificado. Último commit main = 17008b5 (tema editorial).
API no ar rodando o build novo (incidente de deploy resolvido em 018ddd4, verificado
live 2026-07-08). e2e 84/84. Tema visual "Editorial sério" aplicado (papel/tinta,
Newsreader+Instrument Sans, cantos retos).

DUAS FEATURES PRONTAS PORÉM DORMENTES (esperando o usuário colar a chave no Render):
- Mercado Pago (pagamento Pix/cartão dos planos) -> env MP_ACCESS_TOKEN. Token de
  PRODUÇÃO já validado por mim na API do MP (está na memória do projeto; NÃO gravar
  em arquivo do repo). É produção: cobrança real, cartões de teste não funcionam.
- Esqueci a senha -> env RESEND_API_KEY (chave grátis em resend.com).

PENDÊNCIAS DO USUÁRIO (só ele faz): MP_ACCESS_TOKEN, RESEND_API_KEY, ADMIN_SENHA,
secret DATABASE_URL no GitHub (backup), e — URGENTE ~fim de julho — resolver a
expiração do Postgres grátis do Render (upgrade OU migrar p/ Neon, ver BACKUP.md).

PRÓXIMOS PASSOS (código): regenerar ícones/og.png no estilo editorial (a marca
ainda é a antiga azul-verde); domínio próprio; landing p/ visitante; escrow real
(split MP, projeto grande). RECOMENDAÇÃO DO ESCOPO: validar com usuários reais antes
de expandir.

QUANDO O USUÁRIO LIGAR O MP: verificar live com
`curl https://alpha-hub-api.onrender.com/api/planos/recursos` -> deve virar
{"pagamentoReal":true} e o botão "Assinar com Pix/cartão" aparece em /planos.
==================================================================

------------------------------------------------------------------
2026-07-08 — Sessão 18: leitura/auditoria de estado (a pedido do usuário)
------------------------------------------------------------------
Novo chat: usuário pediu "ler toda a pasta, projeto e o site". Feita releitura
do código (schema Prisma, app.module, home web, tema editorial) e VERIFICAÇÃO
AO VIVO do ambiente no Render (serviços estavam dormindo — 503 — e foram
acordados).

ESTADO CONFIRMADO NO AR (2026-07-08 ~16:13 UTC):
- API /api/health -> 200 {"status":"ok"}. Web / -> 200 (title/hero corretos).
- Features gated seguem DORMENTES: /api/planos/recursos -> {"pagamentoReal":false};
  /api/auth/recursos -> {"esqueciSenha":false}. Ou seja: MP_ACCESS_TOKEN e
  RESEND_API_KEY ainda NÃO foram colados no painel do Render (pendência do usuário).
- Dados públicos OK: /api/solicitacoes retorna 2 solicitações seed (1 em
  EM_NEGOCIACAO com 1 proposta); /api/planos lista catálogo (Comunidade grátis,
  Verificado Pro R$29,90, etc.).
- Nada foi alterado no código nesta sessão — apenas leitura e verificação.
Pendências e próximos passos permanecem os já registrados (ver handoff da Sessão 17
e memória do projeto): MP_ACCESS_TOKEN, RESEND_API_KEY, ADMIN_SENHA, secret
DATABASE_URL, e — URGENTE ~fim de julho — expiração do Postgres grátis do Render.
==================================================================

------------------------------------------------------------------
2026-07-08 — Sessão 19: novo layout "Alpha Control" (azul + prata, dark/light)
------------------------------------------------------------------
O usuário entregou um handoff de design (zip "Design taste frontend hub" →
design_handoff_alpha_control/) e pediu "integre o layout". Tela de referência:
"Alpha Hub.dc.html" (Marketplace/Solicitação/Conversas/Planos). A NOTA DE
INTEGRAÇÃO do handoff é explícita: trocar os tokens do globals.css pelo design
system Alpha Control e ADICIONAR tema dark; componentes TSX não precisam de
mudança estrutural. Substitui o tema "Editorial sério" (Sessão 17).

O QUE MUDOU (camada de tokens + tema, mesma API de classes):
- apps/web/src/app/globals.css REESCRITO no sistema "Alpha Control":
  * DARK é o tema padrão (:root) + LIGHT via :root[data-theme="light"].
    Dark: bg #0b0d12 / surface #12151d / fg #f1f3f8 / border #262b38.
    Light: bg #f5f7fb / surface #fff / fg #12151c / border #e1e4ec.
  * Acento azul único: #5b82ff (dark) / #2d5bea (light); --accent-fg calculado
    por contraste (tinta escura sobre o azul claro do dark — passa AA).
  * Tipografia: Space Grotesk (títulos) + IBM Plex Sans (interface) + IBM Plex
    Mono (números/rótulos). Import do Google Fonts trocado.
  * Raios do produto: cards 14px, botões/inputs 9px, pills 999px (badges/chips).
  * Todos os nomes de variável que os TSX referenciam direto foram MANTIDOS
    (--primary=ink [par dos gradientes], --accent, --text, --text-dim,
    --surface-2, --bg-soft, --border, --warn) — nada quebrou.
  * Classes atualizadas p/ fidelidade: badges (pill tingido por status),
    chat (bolha "mine"=azul/accent-fg, "theirs"=panel-2), avatar (accent-tint),
    inputs (focus ring accent), skeleton (base/sheen por tema), nav translúcida.
- Nav.tsx: logotipo ALPHA (gradiente prata) + CONTROL (gradiente azul) + tag
  "HUB" (mono), no lugar do texto "Alpha Hub". + <ThemeToggle/> sempre visível.
- ThemeToggle.tsx (NOVO): alterna dark/light, persiste em localStorage['acTheme']
  (mesma chave dos outros produtos Alpha Control). Ícone sol/lua em SVG inline
  (sem dependência de icon-font nova).
- layout.tsx: script inline no <head> aplica data-theme ANTES da 1ª pintura
  (anti-flash, padrão dark); <html suppressHydrationWarning>; viewport themeColor
  claro/escuro + colorScheme "dark light".
- manifest.ts: background/theme_color → #0b0d12 (dark).
- offline/page.tsx: paleta inline atualizada p/ Alpha Control dark.
- public/sw.js: VERSION ah-v1 → ah-v2 (invalida cache antigo p/ usuários recorrentes).
- .claude/launch.json: adicionada config "web-preview" (porta 3007) — a 3000
  estava ocupada por OUTRO projeto (Desktop\ERP, Alpha Control ERP em next start);
  NÃO foi tocado.

VERIFICAÇÃO (dev server local :3007, via estilos computados — screenshot instável
nesta máquina):
- next build de PRODUÇÃO VERDE (23 rotas).
- Dark (padrão): body #0b0d12, card #12151d, fg #f1f3f8. Light (toggle): body
  #f5f7fb, h1 #12151c, botão primário #2d5bea. Toggle troca data-theme e grava
  acTheme; sem flash no load. Logo com gradientes prata/azul + Space Grotesk OK.
- Único erro no console = GET localhost:3001/api/solicitacoes falhando (API local
  não estava no ar; a home não tem catch nessa chamada) — ambiental, pré-existente.
- Nada na API/NestJS foi alterado.

NÃO FEITO (fora do escopo "camada de tokens"; possíveis follow-ups):
- Ícones/favicon/og.png ainda são a marca ANTIGA (Alpha Hub A azul-verde) — agora
  destoam do logo ALPHA CONTROL in-app. Regenerar no estilo Alpha Control.
- Ícones Phosphor por toda a UI (sino/busca/enviar seguem emoji/texto); filtros de
  categoria seguem <select> em vez de pills. Divergências menores do protótipo.

NÃO COMMITADO/DEPLOYADO: mudanças no working tree; deploy é por git push (o usuário
decide quando publicar no Render).
==================================================================

------------------------------------------------------------------
2026-07-10 — Sessão 20: "mate tudo" — ícones/OG novos + landing de visitante + deploy
------------------------------------------------------------------
Usuário: "comece pelo mais urgente e mate tudo". Executei tudo que NÃO depende da
conta dele; o resto ficou com passos exatos (ver PENDÊNCIAS abaixo).

1) URGENTE (dados/Postgres) — auditado, já estava turnkey:
   - .github/workflows/backup.yml faz pg_dump 16 + gzip diário (06:00 UTC),
     artifact 14 dias. BACKUP.md tem manual + Neon + upgrade. NADA a mudar no
     código. Falta só AÇÃO DO USUÁRIO (conta): colar secret DATABASE_URL no
     GitHub e rodar 1x; e resolver a expiração ~fim de julho (upgrade OU Neon).

2) Ícones/favicon/OG regenerados na marca "Alpha Control" (antes eram a antiga
   A azul-verde, que destoava do logo novo):
   - public/icon.svg + icon-maskable.svg: quadrado gradiente azul (#6b8cff→#2d5bea)
     + "A" geométrico em ink #0a0d14 (= accent + accent-fg do design).
   - PNGs regerados via canvas do browser (sem sharp/canvas na máquina): icon-192
     (192²), icon-512 (512²), apple-icon (180², full-bleed), og.png (1200×630,
     card dark com lockup ALPHA/CONTROL+HUB, headline Space Grotesk, URL mono).
     Método: página temp public/__icongen.html desenha em canvas + rota temp
     /api/iconsave grava os PNG; AMBAS removidas depois. Verificado por Read das
     imagens (renderizam certo) e dimensões conferidas.

3) Landing de visitante (antes caía direto no feed):
   - components/FeedSolicitacoes.tsx (NOVO): feed extraído (busca + filtro +
     recomendadas + lista), reutilizável. Deep-link ?q=/?categoria= preservado.
   - components/Landing.tsx (NOVO): hero + "Como funciona" (4 passos) +
     "Por que o Alpha Hub" (4 diferenciais, 1º em accent-tint) + categorias
     (chips → /?categoria=X#solicitacoes) + faixa CTA azul + feed público embutido
     (#solicitacoes) — navegação pública do núcleo preservada.
   - app/page.tsx: ramifica — carregando→skeleton; visitante→<Landing/>;
     logado→cabeçalho enxuto + <FeedSolicitacoes/>. Removido em-dash da copy
     herdada (regra do design system = zero em-dash).

VERIFICAÇÃO: next build de PRODUÇÃO VERDE (23 rotas; / passou p/ 4.21kB). Landing
conferida no dev :3007 (dark): faixa CTA #5b82ff c/ ink #0a0d14, passos accent-tint,
H1 Space Grotesk; sem erros de console. Screenshot segue instável (usei estrutura +
estilos computados + Read das imagens). API local fora do ar = únicas falhas de fetch.

DEPLOY: commitado e PUSH na main (usuário autorizou "mate tudo") — Render publica só
o serviço web (mudanças 100% web; sem API/migrations). Ver verificação live no fim.

PENDÊNCIAS QUE SÓ O USUÁRIO FAZ (não são código; dei os passos):
- Render→alpha-hub-api→Environment: MP_ACCESS_TOKEN (liga pagamento), RESEND_API_KEY
  (liga esqueci-senha), ADMIN_SENHA forte (fecha admin).
- GitHub secret DATABASE_URL (liga backup) + rodar o Action 1x.
- URGENTE ~fim de julho: expiração do Postgres grátis do Render (upgrade OU Neon).
==================================================================

------------------------------------------------------------------
2026-07-10 — Sessão 21: nav responsiva + robustez do feed (continuar projeto)
------------------------------------------------------------------
Usuário: "continue o projeto" (domínio fica p/ depois). Dois ganhos de baixo risco
sobre o layout recém-lançado:
- NAV RESPONSIVA: com muitos links + toggle + sino, a barra ficava apertada no
  celular. Nav.tsx agora tem menu colapsável — abaixo de 860px os links + ações
  viram um dropdown full-width via botão hambúrguer (ícone ☰/✕); brand + toggle
  ficam sempre visíveis. CSS: .nav-collapse/.nav-hamburger/.nav-chip-hide +
  media query (max-width:860px) em globals.css. Fecha o menu ao clicar em link.
- ROBUSTEZ DO FEED: FeedSolicitacoes buscava /solicitacoes sem catch — se a API
  caía, virava unhandled rejection (o "1 error" do overlay). Agora tem try/catch
  + estado de erro ("Não foi possível carregar… / Tentar de novo") que rechama carregar().
VERIFICAÇÃO (dev :3007): next build VERDE (23 rotas; / = 4.33kB). Desktop (1280):
hambúrguer escondido, links inline. Mobile (375): hambúrguer visível, links
colapsam, clique abre dropdown full-width ancorado abaixo do header (links
empilhados). Feed com API local fora agora mostra o estado de erro (sem rejeito).
DEPLOY: commitado + push na main (mudanças 100% web).
==================================================================

------------------------------------------------------------------
2026-07-10 — Sessão 22: fidelidade ao protótipo (filtros, ícones)
------------------------------------------------------------------
Usuário: "go" (seguir com fidelidade + qualidade). Marketplace mais parecido com
o protótipo Alpha Hub.dc.html, e ícones consistentes:
- ÍCONES SVG inline (NOVO components/icons.tsx): IconSearch, IconBell, IconSend.
  Self-contained (sem icon-font/CDN — preserva PWA/offline e evita problema de CSP).
- FILTROS do feed (FeedSolicitacoes): <select> de categoria virou PILLS (Todas +
  5 categorias reais; ativa = borda 1.5px na cor do texto), e a busca virou uma
  search-box com ícone de lupa e focus-ring accent. Deep-link ?categoria= mantido.
  CSS: .filtros/.search-box/.cat-pill(.ativo)/.cont-resultados em globals.css.
- SINO (NotificationBell): emoji 🔔 -> <IconBell/>.
- CHAT (Chat.tsx): botão "Enviar" -> botão-ícone quadrado (avião de papel), como
  no protótipo.
VERIFICAÇÃO (dev :3007): next build VERDE (23 rotas). 6 pills com "Todas" ativa por
padrão; clicar "design" ativa só ela e URL vira /?categoria=design#solicitacoes;
ícone de lupa presente; search-box raio 9px com focus-ring. Emojis 🔎/⚠️ dos
estados vazios/erro mantidos de propósito (aceitáveis).
DEPLOY: commitado + push na main (100% web).
==================================================================

------------------------------------------------------------------
2026-07-10 — Sessão 23: varredura de QA/polish no tema novo
------------------------------------------------------------------
Usuário: "go". Auditoria das telas restantes no tema Alpha Control. Grep confirmou
que só layout(meta)/offline(paleta nova)/error(tint vermelho) têm cor hardcoded —
o resto flui por tokens, tema chega em tudo. NÃO fiz login (regra: não digitar senha
p/ autenticar) — telas autenticadas revisadas por CÓDIGO; públicas conferidas no ar.
Correções aplicadas:
- Avaliar.tsx: estrela vazia var(--border) -> var(--text-faint) (sumia no dark).
- ReputacaoBadge.tsx: pill de reputação agora verde (var(--ok)/ok-border) com ★,
  como no protótipo (antes era chip neutro).
- planos/page.tsx: plano em DESTAQUE agora realça em AZUL (var(--accent)) em vez de
  branco (--primary); chip "popular" em accent; preço em Space Grotesk 700 (fidelidade).
Telas conferidas por código e OK (tokens, estados vazio/erro/carregando): detalhe da
solicitação (propostas+chat, fiel ao protótipo), login (tabs), planos. Demais telas
(indicações, organizações, perfil, admin, admin/bi, comunidade, contratos, usuários)
usam as mesmas classes/tokens; admin/bi e usuários usam gradiente primary->accent
(ink->azul), OK no dark.
VERIFICAÇÃO: next build VERDE (23 rotas). Planos conferido no ar em dark.
DEPLOY: commitado + push na main (100% web).
==================================================================

------------------------------------------------------------------
2026-07-11 — Sessão 24: validação real de CNPJ (Parte III, 03.08)
------------------------------------------------------------------
Usuário: "go" (feature concreta do escopo). Escolhida a verificação avançada de
identidade/empresa. O fluxo de verificação de organização JÁ existia ponta a ponta
(pedir -> staff aprova/rejeita -> selo + notificação + auditoria, front + back +
admin). O GAP: o CNPJ só era validado por TAMANHO (14-18 chars) — um número no
formato certo mas FALSO (dígitos verificadores errados) passava e entrava na fila.
MUDANÇA (backend, primeira alteração de API desde o reskin):
- NOVO common/validacao/cnpj.ts: cnpjValido() (valida os 2 dígitos verificadores +
  rejeita sequências repetidas), normalizarCnpj(), formatarCnpj(), e o decorator
  class-validator @ECnpj().
- DTOs (organizacoes): documento agora usa @ECnpj() no lugar de @MinLength/@MaxLength
  (CreateOrganizacaoDto e PedirVerificacaoDto).
- Service: CNPJ normalizado p/ o formato canônico "11.222.333/0001-81" antes de
  gravar (criar + pedirVerificacao) -> o índice @unique do documento passa a ser
  consistente (mascarado e sem máscara viram o mesmo valor).
- NOVO cnpj.spec.ts (6 casos): válido c/ e sem máscara, dígito errado, sequência
  repetida, tamanho/lixo, normalização, formatação.
VERIFICAÇÃO: nest build exit 0; jest unit VERDE (2 suites, 9 testes). Não dá p/
testar no ar sem login (guard roda antes do pipe -> 401), mas a lógica está coberta
por unit test. Sem migration (schema não mudou) -> deploy da API é seguro.
DEPLOY: commitado + push na main (API redeploya; confirmar /api/health no ar).
==================================================================

------------------------------------------------------------------
2026-07-11 — Sessão 25: e2e local restaurado + cobertura e2e do CNPJ
------------------------------------------------------------------
Usuário: "go". Objetivo: destravar verificação ponta a ponta (e2e) p/ features
autenticadas. Dois achados/consertos:

1) AMBIENTE E2E LOCAL ESTAVA QUEBRADO (pré-existente, NÃO afeta produção):
   - node_modules em estado "hoisted" híbrido: @nestjs/common estava só na RAIZ
     (órfã, package.json raiz SEM deps) e faltava em apps/api/node_modules; o
     class-validator estava em apps/api. Em runtime o @nestjs/common da raiz não
     enxergava o class-validator -> ValidationPipe do e2e falhava ("class-validator
     package is missing"). É o "estado híbrido" que a doc alerta.
   - CONSERTO (só local; prod instala limpo por app): subi o Postgres portátil
     (scripts/pg-portable.ps1 start) e rodei `npm install` DENTRO de apps/api ->
     node_modules auto-contido (agora tem @nestjs/common E class-validator juntos).
   - Resultado: `npm run test:e2e` volta a rodar. Suíte completa 85/85 VERDE (11 specs).

2) MEU COMMIT 980ec18 (validação de CNPJ) TINHA QUEBRADO UM TESTE E2E:
   organizacoes.e2e-spec usava CNPJ FALSO (`12.345.678/0001-${stamp%100}`), que
   agora é 400 (dígitos inválidos). A produção estava OK, mas a suíte ficou vermelha.
   Corrigido + expandido:
   - helper cnpjValidoDe(base12) gera CNPJ válido e ÚNICO por execução (respeita @unique).
   - novos casos: pedido sem CNPJ -> 400; CNPJ com dígito errado ('11.222.333/0001-80')
     -> 400; CNPJ válido -> 201 PENDENTE + documento NORMALIZADO ("00.000.000/0000-00").
LIÇÃO: rodar o e2e ANTES de dar push em mudança de API (o unit test não pegou isso
porque o guard/pipe não roda no unit). Ambiente e2e local agora funcional p/ próximas
features autenticadas.
VERIFICAÇÃO: e2e organizacoes 10/10; suíte completa 85/85 verde.
DEPLOY: commit do teste (sem mudança de comportamento; API redeploya inócuo).
==================================================================

------------------------------------------------------------------
2026-07-11 — Sessão 26: convite de organização por link (Parte III)
------------------------------------------------------------------
Usuário: "go" (feature concreta, com o harness e2e agora funcionando). Antes só
dava p/ adicionar quem JÁ tinha conta, por e-mail. Agora há convite por LINK.

SCHEMA (migration 20260711073930_convites_organizacao — ADITIVA, tabela nova):
- model ConviteOrganizacao: tokenHash (sha-256; token cru só no link), papel
  (MEMBRO|ADMIN, nunca DONO), expiraEm, usadoEm/usadoPorId, criadoPorId, orgId.
BACKEND (módulo organizacoes):
- POST /organizacoes/:id/convites (DONO/ADMIN) -> gera token (retornado UMA vez),
  papel e validade (1-30 dias, default 7). Audita.
- GET /organizacoes/convite/:token -> prévia (org, papel, expirado?, usado?).
- POST /organizacoes/convite/:token/aceitar -> usuário logado entra na org com o
  papel do convite. Uso ÚNICO (marca usado via updateMany guard atômico), rejeita
  expirado (410), já-usado (409), já-membro (409), token inválido (404). Notifica
  quem criou + audita.
FRONTEND:
- /organizacoes (gestor): bloco "Convidar por link" -> gera e copia o link.
- NOVA /organizacoes/convite/[token] (+layout noindex): prévia + "Aceitar e entrar";
  se deslogado, pede login.
VERIFICAÇÃO: nest build + next build VERDES. e2e organizacoes 15/15 (5 casos novos
de convite: gerar/403, token inválido/404, preview, aceitar+uso único/409, já-membro/409).
Página de aceite conferida no dev (estado deslogado, sem erro de console, dark).
DEPLOY: commit + push na main. API redeploya e roda `prisma migrate deploy` (cria a
tabela nova) — aditivo e seguro. Confirmar no ar: /api/health + Swagger com os endpoints.
==================================================================

------------------------------------------------------------------
2026-07-11 — Sessão 27: listar + revogar convites de organização
------------------------------------------------------------------
Usuário: "go". Follow-up do convite por link: o gestor agora VÊ e REVOGA os links.
- API (sem migration, tabela já existe):
  * GET /organizacoes/:id/convites (DONO/ADMIN) -> lista com status derivado
    (ATIVO/USADO/EXPIRADO), papel, validade, quem criou. NÃO expõe o token (só o
    hash é guardado; o link cru é revelado 1x na criação).
  * DELETE /organizacoes/:id/convites/:conviteId (DONO/ADMIN) -> revoga (apaga);
    links pendentes deixam de valer. Audita.
- Web (/organizacoes): lista de convites com pill de papel + badge de status e
  botão "Revogar" nos ATIVOS; recarrega ao abrir a org e após gerar/revogar.
- e2e (+2): lista (gestor)/403 não-gestor; revoga ativo -> aceite depois dá 404.
VERIFICAÇÃO: nest build + next build VERDES. Suíte e2e COMPLETA 92/92 (organizacoes
17/17). Reiniciei o Postgres portátil local (tinha parado no fim da sessão anterior).
DEPLOY: commit + push na main (API endpoints novos + web; SEM migration).
==================================================================

------------------------------------------------------------------
2026-07-11 — Sessão 28: edição de perfil (nome + bio "sobre você")
------------------------------------------------------------------
Usuário: "go". GAP achado: o campo User.bio é EXIBIDO no perfil público, mas não
havia endpoint nem UI para o usuário DEFINIR/editar a própria bio/nome (register
aceita bio, mas o form de cadastro nunca envia) -> toda bio ficava vazia p/ sempre.
- API (módulo perfil; sem migration, colunas já existem):
  * GET /perfil/me agora também retorna nome e bio (além de portfólio/serviços).
  * PATCH /perfil/me (NOVO): atualiza nome (2-120) e/ou bio (<=600). Bio só com
    espaços -> limpa (null). Trim. Audita antes/depois. (JWT valida contra o banco,
    então nome novo reflete na hora; nome não vive no token.)
- Web:
  * lib/auth.tsx: novo atualizarUsuario(parcial) -> atualiza o nome no nav sem reload.
  * /perfil: card "Sobre você" (nome público + bio com contador 0/600 + Salvar);
    popula do GET /perfil/me; ao salvar, PATCH + atualiza estado e o nome do nav.
- e2e (perfil +3): atualiza nome/bio -> reflete no público; bio em branco -> null;
  validação (nome curto/bio longa -> 400) + exige login (401).
VERIFICAÇÃO: nest build + next build VERDES. Suíte e2e COMPLETA 95/95 (perfil 10/10).
UI de edição fica atrás de login (não testável no ar sem logar), mas coberta por e2e.
DEPLOY: commit + push na main (API + web; SEM migration).
==================================================================
